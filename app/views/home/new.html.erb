<%= form_tag("/home/create") do %>

<% @post.errors.full_messages.each do |message| %>
          <div class="form-error">
            <%= message %>
          </div>
        <% end %>

<textarea name="context"></textarea>
<input type="submit" value="投稿">
<% end %>
<%= link_to("投稿一覧","/home/top") %>
